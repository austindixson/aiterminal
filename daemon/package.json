{
  "name": "@aiterminal/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "AITerminal gateway daemon — job queue, multi-agent loop, TCP control plane",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
